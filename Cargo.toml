[package]
name = "herdr-annotator"
version = "0.11.0"
edition = "2021"
license = "MIT"
description = "Herdr plugin: an agent-summoned, blocking diff-review pane with line annotations."
repository = "https://github.com/JonasBaeumer/herdr-file-annotator"

[[bin]]
name = "herdr-annotator"
path = "src/main.rs"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
crossterm = "0.28"
ratatui = "0.29"
# fancy-regex backend: pure Rust, no oniguruma C dependency
syntect = { version = "5", default-features = false, features = ["default-fancy"] }
# bat's curated syntax set precompiled for syntect: ~213 languages
# (TypeScript/TSX included) without vendoring grammar files. The
# syntect-fancy feature matches syntect's default-fancy regex backend.
two-face = { version = "0.5", default-features = false, features = ["syntect-fancy"] }
# display columns (unicode-width, ratatui's own model) + UAX #29 cluster
# atomicity (unicode-segmentation); both pure Rust
unicode-segmentation = "1"
unicode-width = "0.2"
toml = "0.8"

[profile.release]
strip = true
